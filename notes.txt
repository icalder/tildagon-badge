as admin:

usbipd.exe list
12-2   303a:1001  USB Serial Device (COM3), USB JTAG/serial debug unit          Not shared
usbipd bind --busid 12-2
usbipd attach --wsl --busid 12-2
...
usbipd detach --busid <BUSID>

# Note
 - need to enable bwrap suid in nixos configuration
 - bwrap example:

 bwrap \
  --ro-bind /nix /nix \
  --proc /proc \
  --dev /dev \
  --tmpfs /tmp \
  --unshare-all \
  --setenv PATH "/run/current-system/sw/bin" \
  --ro-bind /run/current-system/sw/bin /run/current-system/sw/bin \
  $(readlink -f $(which bash))


For the flake we use: https://github.com/michalrus/esp-rust-nix-sandbox

cd embassy_blinky
cargo build --release
cargo doc --release --open
espflash save-image --chip esp32 target/xtensa-esp32-none-elf/release/embassy_blinky out.bin

To flash, and monitor output:
  • cargo espflash flash --monitor --features esp32 --target xtensa-esp32-none-elf --release
  • cargo run --release (alias of ^)
  • picocom --baud=115200 --imap lfcrlf /dev/ttyUSB0

Or, using the Cargo root project:

cargo build -p embassy_blinky --features esp32s3 --target xtensa-esp32s3-none-elf --release
cargo run -p embassy_blinky --release

cargo build -p embassy_wifi_ble --features esp32s3 --target xtensa-esp32s3-none-elf --release
cargo run -p embassy_wifi_ble --release

# Building a firmware bin file
espflash save-image --chip esp32s3 target/xtensa-esp32s3-none-elf/release/itag_finder firmware.bin

# PWA for Bluetooth LED Control
https://icalder.github.io/tildagon-badge/pwa/ble-led-demo.html